<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:gravity="center"
    android:orientation="vertical">

    <ImageView
        android:id="@+id/img_icon"
        android:layout_width="180.0dip"
        android:layout_height="180.0dip"
        android:scaleType="fitCenter"
        android:src="@drawable/ico_network_error" />

    <TextView
        android:id="@+id/tv_detail"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_marginTop="32.0dip"
        android:gravity="center"
        android:text="无法连接网络"
        android:textColor="@color/skin_primary_text"
        android:textSize="16.0sp" />

    <Button
        android:id="@+id/btn_retry"
        android:layout_width="185.0dip"
        android:layout_height="40.0dip"
        android:layout_marginTop="8.0dip"
        android:background="@drawable/skin_common_widget_inverse"
        android:clickable="true"
        android:focusable="true"
        android:text="点击重试"
        android:textColor="@drawable/skin_headline_text_white"
        android:textSize="14sp" />
</LinearLayout>